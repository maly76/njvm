void main()
{
    local Integer x;
    local Integer y;
    local Integer counter;
    local Integer result;
    counter = 0;
    result = 0;
    x = readInteger();
    y = readInteger();

    while (counter < x)
    {
        result = result + y;
        counter = counter + 1;
    }
    writeInteger(y);
    writeCharakter('\n');
}